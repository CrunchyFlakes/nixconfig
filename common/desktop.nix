# Common desktop configuration that can be imported by system-specific desktop configs
{
  imports = [ ./devel.nix ];
  # This file is intentionally left blank to be extended by system-specific configs
}